---
# FIXTURE L9 — ANCIEN profil d'exploitation (forme `ansible-role-grav-site`).
# SYNTHÉTIQUE, aucune donnée réelle. Adresses TEST-NET-1.
#
# Ancien modèle : un profil = un jeu de variables `grav_*` à PLAT sous
# group_vars/grav_servers/, jamais indexées par nom d'hôte.
all:
  children:
    grav_servers:
      hosts:
        old-lavallee:
          ansible_host: 192.0.2.41
