---
# FIXTURE DE TEST — volontairement incohérente.
# Utilisée par GSO-T06 pour prouver que registry_lint détecte les écarts
# (garde-fou rejouable). Ne représente aucune cible réelle.
all:
  children:
    grav_servers:
      hosts:
        fixture-host-a:
          ansible_host: 192.0.2.201
        fixture-host-b:
          ansible_host: 192.0.2.202
