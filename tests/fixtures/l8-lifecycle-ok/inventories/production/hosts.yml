---
# FIXTURE L8 — arborescence de production SYNTHÉTIQUE et cohérente.
# Aucune donnée réelle. Adresses TEST-NET-1. Jamais utilisée en exploitation.
#
# État modélisé :
#   - grav-alpha : actif, jamais retiré
#   - grav-delta : actif — a été retiré puis RÉACTIVÉ une fois
#   - grav-gamma : actuellement retiré
#   - grav-omega : actuellement retiré (retiré une 2e fois après une
#                  réactivation antérieure)
all:
  children:
    grav_servers:
      hosts:
        grav-alpha:
          ansible_host: 192.0.2.31
        grav-delta:
          ansible_host: 192.0.2.34
