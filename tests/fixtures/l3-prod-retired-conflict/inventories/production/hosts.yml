---
# FIXTURE de test L3 — état INCOHÉRENT : `grav-beta` est actif dans
# l'inventaire ET listé comme retiré. Toute sélection de `grav-beta` doit
# être refusée (GSO-REQ-052).
all:
  children:
    grav_servers:
      hosts:
        grav-alpha:
          ansible_host: 192.0.2.21
        grav-beta:
          ansible_host: 192.0.2.22
