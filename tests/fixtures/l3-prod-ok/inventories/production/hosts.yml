---
# FIXTURE de test L3 — arborescence de production SYNTHÉTIQUE et isolée.
# Aucune donnée réelle. Adresses TEST-NET-1. Jamais utilisée en exploitation.
all:
  children:
    grav_servers:
      hosts:
        grav-alpha:
          ansible_host: 192.0.2.21
        grav-beta:
          ansible_host: 192.0.2.22
