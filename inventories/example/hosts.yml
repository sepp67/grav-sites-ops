---
# Inventaire d'EXEMPLE — lot L1.
#
# Usage : tests de cohérence, syntax-checks, documentation. NE DOIT JAMAIS
# servir d'inventaire de production (contrat, section 8.7, GSO-REQ-006,
# GSO-REQ-058).
#
# Toutes les adresses sont dans TEST-NET-1 (192.0.2.0/24, RFC 5737) : non
# routables, purement documentaires (GSO-REQ-068, GSO-REQ-100). Aucune
# donnée d'infrastructure réelle.
#
# Deux hôtes fictifs, chacun représentant une VM dédiée à un projet
# (GSO-REQ-007, GSO-REQ-036). Le nom d'hôte est l'unique clé de jointure
# avec le registre et, plus tard, le vault (GSO-REQ-008, GSO-REQ-037).

all:
  children:
    grav_servers:
      hosts:
        grav-example-alpha:
          ansible_host: 192.0.2.11
        grav-example-beta:
          ansible_host: 192.0.2.12
